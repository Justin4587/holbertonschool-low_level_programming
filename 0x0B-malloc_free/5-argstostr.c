char *argstostr(int ac, char **av)
{
int i = 0;
char *dup;
 int s = 0;
 int acn = ac;
 int smain =0;


if (ac == 0)
  return (NULL);


 while (i++ < ac; acn--)
   {
     while (*av++)
      av[acn] != '\0)
      smain++; 





dup = malloc(sizeof(





dup = malloc(sizeof(char) * (count1 + count2 + 1));

if (dup == NULL)
return (NULL);

for (i = 0; i < count1 ; i++)
dup[i] = s1[i];

for (j = 0; j <= count2 ; j++)
dup[j + i] = s2[j];

return (dup);
}




