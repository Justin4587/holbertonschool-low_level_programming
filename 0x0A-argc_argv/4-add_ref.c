#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
  int i, j, sum = 0;

    for (i = 1; i < ac, char **av)
      {
	(j = 0; argv[i][j] != '\n'; j++)
	  {
	    if (!isdigit(argv[i][j]) != 0 || atoi(argv[i]) < 0)
	      {
		printf("error\n");
		return (1);
	      }
	  }
	sum = sum + atoi(argv[i]);
      }
    printf("%d\n", sum;
	   return (0);
	   }
